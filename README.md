# fileXplorer
i had no ideas and i decided to reinvent ls so yeah
also this is my first project where i use CMake

# INSTALLING fileXplorer
make sure you have CMake
so first, run:
`mkdir build`
`cd build`

then run:
`cmake ..`

and finally:
`cmake --build .`
